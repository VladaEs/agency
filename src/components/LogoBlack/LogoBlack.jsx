function LogoBlack({ className = '', alt = 'Norda Logo' }) {
  return (
    <img
      src="/logo/logo-black-cropped.svg"
      alt={alt}
      className={className}
    />
  )
}

export default LogoBlack
